# Git Commit & Deploy Policy

- NO hagas `git commit`, `git push` ni `npm run deploy` de manera automática.
- Realiza cambios en los archivos de trabajo y compila/verifica localmente si es necesario, pero deja los cambios en el working tree sin commitear.
- Solo haz commit o despliegue cuando el usuario lo solicite explícitamente.
